[package]
name = "compiler-generator"
version = "0.1.0"
edition = "2024"

[features]
default = ["debug"]
debug = []

[dependencies]
clap = { version = "4.6.7", features = ["derive"] }
regex = "1.13.1"
